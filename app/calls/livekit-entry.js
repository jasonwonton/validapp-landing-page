// This entry is bundled into a self-hosted, lazy-loaded production asset by
// scripts/build-static.mjs. Keeping the vendor dependency out of the app shell
// avoids penalizing users who never place a call.
export { Room, RoomEvent, Track } from "livekit-client";
